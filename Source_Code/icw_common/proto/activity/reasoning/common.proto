syntax = "proto3";

package icw.activity.reasoning;

option go_package = "icw_common/gen/activity/reasoning;reasoningpb";

// 图像检测推理产物上传策略结构体
message ReasoningArtifactUploadPolicy {
  string url = 1;
  string key_prefix = 2;
  map<string, string> form_data = 3;
}
