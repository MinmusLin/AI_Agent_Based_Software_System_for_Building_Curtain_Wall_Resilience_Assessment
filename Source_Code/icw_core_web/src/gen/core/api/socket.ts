// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: core/api/socket.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "icw.core.api";

/** CreateSocketTicket 请求结构体 */
export interface CreateSocketTicketRequest {
  project_id: string;
  scope: string;
}

/** CreateSocketTicket 响应结构体 */
export interface CreateSocketTicketResponse {
  ticket: string;
  expires_in: number;
}

/** SetupWebSocket 请求结构体 */
export interface SetupWebSocketRequest {
  /** 从 HTTP Query 获取 */
  project_id: string;
  /** 从 HTTP Query 获取 */
  scope: string;
  /** 从 HTTP Query 获取 */
  ticket: string;
}

/** SetupWebSocket 响应结构体 */
export interface SetupWebSocketResponse {
}

function createBaseCreateSocketTicketRequest(): CreateSocketTicketRequest {
  return { project_id: "", scope: "" };
}

export const CreateSocketTicketRequest: MessageFns<CreateSocketTicketRequest> = {
  encode(message: CreateSocketTicketRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project_id !== "") {
      writer.uint32(10).string(message.project_id);
    }
    if (message.scope !== "") {
      writer.uint32(18).string(message.scope);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSocketTicketRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSocketTicketRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.scope = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSocketTicketRequest {
    return {
      project_id: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
      scope: isSet(object.scope) ? globalThis.String(object.scope) : "",
    };
  },

  toJSON(message: CreateSocketTicketRequest): unknown {
    const obj: any = {};
    if (message.project_id !== "") {
      obj.projectId = message.project_id;
    }
    if (message.scope !== "") {
      obj.scope = message.scope;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateSocketTicketRequest>): CreateSocketTicketRequest {
    return CreateSocketTicketRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateSocketTicketRequest>): CreateSocketTicketRequest {
    const message = createBaseCreateSocketTicketRequest();
    message.project_id = object.project_id ?? "";
    message.scope = object.scope ?? "";
    return message;
  },
};

function createBaseCreateSocketTicketResponse(): CreateSocketTicketResponse {
  return { ticket: "", expires_in: 0 };
}

export const CreateSocketTicketResponse: MessageFns<CreateSocketTicketResponse> = {
  encode(message: CreateSocketTicketResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ticket !== "") {
      writer.uint32(10).string(message.ticket);
    }
    if (message.expires_in !== 0) {
      writer.uint32(16).int64(message.expires_in);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSocketTicketResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSocketTicketResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ticket = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expires_in = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSocketTicketResponse {
    return {
      ticket: isSet(object.ticket) ? globalThis.String(object.ticket) : "",
      expires_in: isSet(object.expiresIn)
        ? globalThis.Number(object.expiresIn)
        : isSet(object.expires_in)
        ? globalThis.Number(object.expires_in)
        : 0,
    };
  },

  toJSON(message: CreateSocketTicketResponse): unknown {
    const obj: any = {};
    if (message.ticket !== "") {
      obj.ticket = message.ticket;
    }
    if (message.expires_in !== 0) {
      obj.expiresIn = Math.round(message.expires_in);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateSocketTicketResponse>): CreateSocketTicketResponse {
    return CreateSocketTicketResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateSocketTicketResponse>): CreateSocketTicketResponse {
    const message = createBaseCreateSocketTicketResponse();
    message.ticket = object.ticket ?? "";
    message.expires_in = object.expires_in ?? 0;
    return message;
  },
};

function createBaseSetupWebSocketRequest(): SetupWebSocketRequest {
  return { project_id: "", scope: "", ticket: "" };
}

export const SetupWebSocketRequest: MessageFns<SetupWebSocketRequest> = {
  encode(message: SetupWebSocketRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project_id !== "") {
      writer.uint32(10).string(message.project_id);
    }
    if (message.scope !== "") {
      writer.uint32(18).string(message.scope);
    }
    if (message.ticket !== "") {
      writer.uint32(26).string(message.ticket);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetupWebSocketRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetupWebSocketRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.scope = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ticket = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetupWebSocketRequest {
    return {
      project_id: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
      scope: isSet(object.scope) ? globalThis.String(object.scope) : "",
      ticket: isSet(object.ticket) ? globalThis.String(object.ticket) : "",
    };
  },

  toJSON(message: SetupWebSocketRequest): unknown {
    const obj: any = {};
    if (message.project_id !== "") {
      obj.projectId = message.project_id;
    }
    if (message.scope !== "") {
      obj.scope = message.scope;
    }
    if (message.ticket !== "") {
      obj.ticket = message.ticket;
    }
    return obj;
  },

  create(base?: DeepPartial<SetupWebSocketRequest>): SetupWebSocketRequest {
    return SetupWebSocketRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetupWebSocketRequest>): SetupWebSocketRequest {
    const message = createBaseSetupWebSocketRequest();
    message.project_id = object.project_id ?? "";
    message.scope = object.scope ?? "";
    message.ticket = object.ticket ?? "";
    return message;
  },
};

function createBaseSetupWebSocketResponse(): SetupWebSocketResponse {
  return {};
}

export const SetupWebSocketResponse: MessageFns<SetupWebSocketResponse> = {
  encode(_: SetupWebSocketResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetupWebSocketResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetupWebSocketResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SetupWebSocketResponse {
    return {};
  },

  toJSON(_: SetupWebSocketResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<SetupWebSocketResponse>): SetupWebSocketResponse {
    return SetupWebSocketResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<SetupWebSocketResponse>): SetupWebSocketResponse {
    const message = createBaseSetupWebSocketResponse();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
