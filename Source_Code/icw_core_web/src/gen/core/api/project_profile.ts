// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: core/api/project_profile.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Project } from "./common";

export const protobufPackage = "icw.core.api";

/** DeleteProjectThumbnail 请求结构体 */
export interface DeleteProjectThumbnailRequest {
  /** 从 HTTP Query 获取 */
  project_id: string;
}

/** DeleteProjectThumbnail 响应结构体 */
export interface DeleteProjectThumbnailResponse {
}

/** GetProjectProfile 请求结构体 */
export interface GetProjectProfileRequest {
  /** 从 HTTP Query 获取 */
  project_id: string;
}

/** GetProjectProfile 响应结构体 */
export interface GetProjectProfileResponse {
  project: Project | undefined;
}

/** GetProjectThumbnail 请求结构体 */
export interface GetProjectThumbnailRequest {
  /** 从 HTTP Query 获取 */
  project_id: string;
}

/** GetProjectThumbnail 响应结构体 */
export interface GetProjectThumbnailResponse {
  thumbnail_url: string;
}

/** UpdateProjectProfile 请求结构体 */
export interface UpdateProjectProfileRequest {
  project_id: string;
  name: string;
  building_name: string;
  building_location: string;
  built_year: number;
  building_description: string;
  known_issues: string;
  assessment_goal: string;
}

/** UpdateProjectProfile 响应结构体 */
export interface UpdateProjectProfileResponse {
  project: Project | undefined;
}

/** UploadProjectThumbnail 请求结构体 */
export interface UploadProjectThumbnailRequest {
  project_id: string;
}

/** UploadProjectThumbnail 响应结构体 */
export interface UploadProjectThumbnailResponse {
  upload_url: string;
}

function createBaseDeleteProjectThumbnailRequest(): DeleteProjectThumbnailRequest {
  return { project_id: "" };
}

export const DeleteProjectThumbnailRequest: MessageFns<DeleteProjectThumbnailRequest> = {
  encode(message: DeleteProjectThumbnailRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project_id !== "") {
      writer.uint32(10).string(message.project_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteProjectThumbnailRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteProjectThumbnailRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteProjectThumbnailRequest {
    return {
      project_id: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
    };
  },

  toJSON(message: DeleteProjectThumbnailRequest): unknown {
    const obj: any = {};
    if (message.project_id !== "") {
      obj.projectId = message.project_id;
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteProjectThumbnailRequest>): DeleteProjectThumbnailRequest {
    return DeleteProjectThumbnailRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteProjectThumbnailRequest>): DeleteProjectThumbnailRequest {
    const message = createBaseDeleteProjectThumbnailRequest();
    message.project_id = object.project_id ?? "";
    return message;
  },
};

function createBaseDeleteProjectThumbnailResponse(): DeleteProjectThumbnailResponse {
  return {};
}

export const DeleteProjectThumbnailResponse: MessageFns<DeleteProjectThumbnailResponse> = {
  encode(_: DeleteProjectThumbnailResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteProjectThumbnailResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteProjectThumbnailResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DeleteProjectThumbnailResponse {
    return {};
  },

  toJSON(_: DeleteProjectThumbnailResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<DeleteProjectThumbnailResponse>): DeleteProjectThumbnailResponse {
    return DeleteProjectThumbnailResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<DeleteProjectThumbnailResponse>): DeleteProjectThumbnailResponse {
    const message = createBaseDeleteProjectThumbnailResponse();
    return message;
  },
};

function createBaseGetProjectProfileRequest(): GetProjectProfileRequest {
  return { project_id: "" };
}

export const GetProjectProfileRequest: MessageFns<GetProjectProfileRequest> = {
  encode(message: GetProjectProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project_id !== "") {
      writer.uint32(10).string(message.project_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProjectProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProjectProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProjectProfileRequest {
    return {
      project_id: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
    };
  },

  toJSON(message: GetProjectProfileRequest): unknown {
    const obj: any = {};
    if (message.project_id !== "") {
      obj.projectId = message.project_id;
    }
    return obj;
  },

  create(base?: DeepPartial<GetProjectProfileRequest>): GetProjectProfileRequest {
    return GetProjectProfileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetProjectProfileRequest>): GetProjectProfileRequest {
    const message = createBaseGetProjectProfileRequest();
    message.project_id = object.project_id ?? "";
    return message;
  },
};

function createBaseGetProjectProfileResponse(): GetProjectProfileResponse {
  return { project: undefined };
}

export const GetProjectProfileResponse: MessageFns<GetProjectProfileResponse> = {
  encode(message: GetProjectProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project !== undefined) {
      Project.encode(message.project, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProjectProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProjectProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project = Project.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProjectProfileResponse {
    return { project: isSet(object.project) ? Project.fromJSON(object.project) : undefined };
  },

  toJSON(message: GetProjectProfileResponse): unknown {
    const obj: any = {};
    if (message.project !== undefined) {
      obj.project = Project.toJSON(message.project);
    }
    return obj;
  },

  create(base?: DeepPartial<GetProjectProfileResponse>): GetProjectProfileResponse {
    return GetProjectProfileResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetProjectProfileResponse>): GetProjectProfileResponse {
    const message = createBaseGetProjectProfileResponse();
    message.project = (object.project !== undefined && object.project !== null)
      ? Project.fromPartial(object.project)
      : undefined;
    return message;
  },
};

function createBaseGetProjectThumbnailRequest(): GetProjectThumbnailRequest {
  return { project_id: "" };
}

export const GetProjectThumbnailRequest: MessageFns<GetProjectThumbnailRequest> = {
  encode(message: GetProjectThumbnailRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project_id !== "") {
      writer.uint32(10).string(message.project_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProjectThumbnailRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProjectThumbnailRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProjectThumbnailRequest {
    return {
      project_id: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
    };
  },

  toJSON(message: GetProjectThumbnailRequest): unknown {
    const obj: any = {};
    if (message.project_id !== "") {
      obj.projectId = message.project_id;
    }
    return obj;
  },

  create(base?: DeepPartial<GetProjectThumbnailRequest>): GetProjectThumbnailRequest {
    return GetProjectThumbnailRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetProjectThumbnailRequest>): GetProjectThumbnailRequest {
    const message = createBaseGetProjectThumbnailRequest();
    message.project_id = object.project_id ?? "";
    return message;
  },
};

function createBaseGetProjectThumbnailResponse(): GetProjectThumbnailResponse {
  return { thumbnail_url: "" };
}

export const GetProjectThumbnailResponse: MessageFns<GetProjectThumbnailResponse> = {
  encode(message: GetProjectThumbnailResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.thumbnail_url !== "") {
      writer.uint32(10).string(message.thumbnail_url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProjectThumbnailResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProjectThumbnailResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.thumbnail_url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProjectThumbnailResponse {
    return {
      thumbnail_url: isSet(object.thumbnailUrl)
        ? globalThis.String(object.thumbnailUrl)
        : isSet(object.thumbnail_url)
        ? globalThis.String(object.thumbnail_url)
        : "",
    };
  },

  toJSON(message: GetProjectThumbnailResponse): unknown {
    const obj: any = {};
    if (message.thumbnail_url !== "") {
      obj.thumbnailUrl = message.thumbnail_url;
    }
    return obj;
  },

  create(base?: DeepPartial<GetProjectThumbnailResponse>): GetProjectThumbnailResponse {
    return GetProjectThumbnailResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetProjectThumbnailResponse>): GetProjectThumbnailResponse {
    const message = createBaseGetProjectThumbnailResponse();
    message.thumbnail_url = object.thumbnail_url ?? "";
    return message;
  },
};

function createBaseUpdateProjectProfileRequest(): UpdateProjectProfileRequest {
  return {
    project_id: "",
    name: "",
    building_name: "",
    building_location: "",
    built_year: 0,
    building_description: "",
    known_issues: "",
    assessment_goal: "",
  };
}

export const UpdateProjectProfileRequest: MessageFns<UpdateProjectProfileRequest> = {
  encode(message: UpdateProjectProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project_id !== "") {
      writer.uint32(10).string(message.project_id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.building_name !== "") {
      writer.uint32(26).string(message.building_name);
    }
    if (message.building_location !== "") {
      writer.uint32(34).string(message.building_location);
    }
    if (message.built_year !== 0) {
      writer.uint32(40).uint32(message.built_year);
    }
    if (message.building_description !== "") {
      writer.uint32(50).string(message.building_description);
    }
    if (message.known_issues !== "") {
      writer.uint32(58).string(message.known_issues);
    }
    if (message.assessment_goal !== "") {
      writer.uint32(66).string(message.assessment_goal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateProjectProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateProjectProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.building_name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.building_location = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.built_year = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.building_description = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.known_issues = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.assessment_goal = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateProjectProfileRequest {
    return {
      project_id: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      building_name: isSet(object.buildingName)
        ? globalThis.String(object.buildingName)
        : isSet(object.building_name)
        ? globalThis.String(object.building_name)
        : "",
      building_location: isSet(object.buildingLocation)
        ? globalThis.String(object.buildingLocation)
        : isSet(object.building_location)
        ? globalThis.String(object.building_location)
        : "",
      built_year: isSet(object.builtYear)
        ? globalThis.Number(object.builtYear)
        : isSet(object.built_year)
        ? globalThis.Number(object.built_year)
        : 0,
      building_description: isSet(object.buildingDescription)
        ? globalThis.String(object.buildingDescription)
        : isSet(object.building_description)
        ? globalThis.String(object.building_description)
        : "",
      known_issues: isSet(object.knownIssues)
        ? globalThis.String(object.knownIssues)
        : isSet(object.known_issues)
        ? globalThis.String(object.known_issues)
        : "",
      assessment_goal: isSet(object.assessmentGoal)
        ? globalThis.String(object.assessmentGoal)
        : isSet(object.assessment_goal)
        ? globalThis.String(object.assessment_goal)
        : "",
    };
  },

  toJSON(message: UpdateProjectProfileRequest): unknown {
    const obj: any = {};
    if (message.project_id !== "") {
      obj.projectId = message.project_id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.building_name !== "") {
      obj.buildingName = message.building_name;
    }
    if (message.building_location !== "") {
      obj.buildingLocation = message.building_location;
    }
    if (message.built_year !== 0) {
      obj.builtYear = Math.round(message.built_year);
    }
    if (message.building_description !== "") {
      obj.buildingDescription = message.building_description;
    }
    if (message.known_issues !== "") {
      obj.knownIssues = message.known_issues;
    }
    if (message.assessment_goal !== "") {
      obj.assessmentGoal = message.assessment_goal;
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateProjectProfileRequest>): UpdateProjectProfileRequest {
    return UpdateProjectProfileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateProjectProfileRequest>): UpdateProjectProfileRequest {
    const message = createBaseUpdateProjectProfileRequest();
    message.project_id = object.project_id ?? "";
    message.name = object.name ?? "";
    message.building_name = object.building_name ?? "";
    message.building_location = object.building_location ?? "";
    message.built_year = object.built_year ?? 0;
    message.building_description = object.building_description ?? "";
    message.known_issues = object.known_issues ?? "";
    message.assessment_goal = object.assessment_goal ?? "";
    return message;
  },
};

function createBaseUpdateProjectProfileResponse(): UpdateProjectProfileResponse {
  return { project: undefined };
}

export const UpdateProjectProfileResponse: MessageFns<UpdateProjectProfileResponse> = {
  encode(message: UpdateProjectProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project !== undefined) {
      Project.encode(message.project, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateProjectProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateProjectProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project = Project.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateProjectProfileResponse {
    return { project: isSet(object.project) ? Project.fromJSON(object.project) : undefined };
  },

  toJSON(message: UpdateProjectProfileResponse): unknown {
    const obj: any = {};
    if (message.project !== undefined) {
      obj.project = Project.toJSON(message.project);
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateProjectProfileResponse>): UpdateProjectProfileResponse {
    return UpdateProjectProfileResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateProjectProfileResponse>): UpdateProjectProfileResponse {
    const message = createBaseUpdateProjectProfileResponse();
    message.project = (object.project !== undefined && object.project !== null)
      ? Project.fromPartial(object.project)
      : undefined;
    return message;
  },
};

function createBaseUploadProjectThumbnailRequest(): UploadProjectThumbnailRequest {
  return { project_id: "" };
}

export const UploadProjectThumbnailRequest: MessageFns<UploadProjectThumbnailRequest> = {
  encode(message: UploadProjectThumbnailRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project_id !== "") {
      writer.uint32(10).string(message.project_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadProjectThumbnailRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadProjectThumbnailRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadProjectThumbnailRequest {
    return {
      project_id: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
    };
  },

  toJSON(message: UploadProjectThumbnailRequest): unknown {
    const obj: any = {};
    if (message.project_id !== "") {
      obj.projectId = message.project_id;
    }
    return obj;
  },

  create(base?: DeepPartial<UploadProjectThumbnailRequest>): UploadProjectThumbnailRequest {
    return UploadProjectThumbnailRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UploadProjectThumbnailRequest>): UploadProjectThumbnailRequest {
    const message = createBaseUploadProjectThumbnailRequest();
    message.project_id = object.project_id ?? "";
    return message;
  },
};

function createBaseUploadProjectThumbnailResponse(): UploadProjectThumbnailResponse {
  return { upload_url: "" };
}

export const UploadProjectThumbnailResponse: MessageFns<UploadProjectThumbnailResponse> = {
  encode(message: UploadProjectThumbnailResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.upload_url !== "") {
      writer.uint32(10).string(message.upload_url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadProjectThumbnailResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadProjectThumbnailResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.upload_url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadProjectThumbnailResponse {
    return {
      upload_url: isSet(object.uploadUrl)
        ? globalThis.String(object.uploadUrl)
        : isSet(object.upload_url)
        ? globalThis.String(object.upload_url)
        : "",
    };
  },

  toJSON(message: UploadProjectThumbnailResponse): unknown {
    const obj: any = {};
    if (message.upload_url !== "") {
      obj.uploadUrl = message.upload_url;
    }
    return obj;
  },

  create(base?: DeepPartial<UploadProjectThumbnailResponse>): UploadProjectThumbnailResponse {
    return UploadProjectThumbnailResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UploadProjectThumbnailResponse>): UploadProjectThumbnailResponse {
    const message = createBaseUploadProjectThumbnailResponse();
    message.upload_url = object.upload_url ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
