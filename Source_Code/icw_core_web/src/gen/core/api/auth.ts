// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: core/api/auth.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  EmailCodeScene_Value,
  emailCodeScene_ValueFromJSON,
  emailCodeScene_ValueToJSON,
  LoginScene_Value,
  loginScene_ValueFromJSON,
  loginScene_ValueToJSON,
  User,
} from "../common";

export const protobufPackage = "icw.core.api";

/** Login 请求结构体 */
export interface LoginRequest {
  email: string;
  scene: LoginScene_Value;
  code: string;
}

/** Login 响应结构体 */
export interface LoginResponse {
  access_token: string;
  expires_in: number;
  user: User | undefined;
}

/** Logout 请求结构体 */
export interface LogoutRequest {
}

/** Logout 响应结构体 */
export interface LogoutResponse {
}

/** Me 请求结构体 */
export interface MeRequest {
}

/** Me 响应结构体 */
export interface MeResponse {
  user: User | undefined;
}

/** Refresh 请求结构体 */
export interface RefreshRequest {
}

/** Refresh 响应结构体 */
export interface RefreshResponse {
  access_token: string;
  expires_in: number;
  user: User | undefined;
}

/** Register 请求结构体 */
export interface RegisterRequest {
  email: string;
  email_code: string;
  password: string;
  name: string;
}

/** Register 响应结构体 */
export interface RegisterResponse {
}

/** ResetPassword 请求结构体 */
export interface ResetPasswordRequest {
  email: string;
  email_code: string;
  new_password: string;
}

/** ResetPassword 响应结构体 */
export interface ResetPasswordResponse {
}

/** SendEmailCode 请求结构体 */
export interface SendEmailCodeRequest {
  email: string;
  scene: EmailCodeScene_Value;
}

/** SendEmailCode 响应结构体 */
export interface SendEmailCodeResponse {
  expires_in: number;
}

function createBaseLoginRequest(): LoginRequest {
  return { email: "", scene: 0, code: "" };
}

export const LoginRequest: MessageFns<LoginRequest> = {
  encode(message: LoginRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.scene !== 0) {
      writer.uint32(16).int32(message.scene);
    }
    if (message.code !== "") {
      writer.uint32(26).string(message.code);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoginRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoginRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.scene = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.code = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoginRequest {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      scene: isSet(object.scene) ? loginScene_ValueFromJSON(object.scene) : 0,
      code: isSet(object.code) ? globalThis.String(object.code) : "",
    };
  },

  toJSON(message: LoginRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.scene !== 0) {
      obj.scene = loginScene_ValueToJSON(message.scene);
    }
    if (message.code !== "") {
      obj.code = message.code;
    }
    return obj;
  },

  create(base?: DeepPartial<LoginRequest>): LoginRequest {
    return LoginRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LoginRequest>): LoginRequest {
    const message = createBaseLoginRequest();
    message.email = object.email ?? "";
    message.scene = object.scene ?? 0;
    message.code = object.code ?? "";
    return message;
  },
};

function createBaseLoginResponse(): LoginResponse {
  return { access_token: "", expires_in: 0, user: undefined };
}

export const LoginResponse: MessageFns<LoginResponse> = {
  encode(message: LoginResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.access_token !== "") {
      writer.uint32(10).string(message.access_token);
    }
    if (message.expires_in !== 0) {
      writer.uint32(16).int32(message.expires_in);
    }
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoginResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoginResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.access_token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expires_in = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoginResponse {
    return {
      access_token: isSet(object.accessToken)
        ? globalThis.String(object.accessToken)
        : isSet(object.access_token)
        ? globalThis.String(object.access_token)
        : "",
      expires_in: isSet(object.expiresIn)
        ? globalThis.Number(object.expiresIn)
        : isSet(object.expires_in)
        ? globalThis.Number(object.expires_in)
        : 0,
      user: isSet(object.user) ? User.fromJSON(object.user) : undefined,
    };
  },

  toJSON(message: LoginResponse): unknown {
    const obj: any = {};
    if (message.access_token !== "") {
      obj.accessToken = message.access_token;
    }
    if (message.expires_in !== 0) {
      obj.expiresIn = Math.round(message.expires_in);
    }
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    return obj;
  },

  create(base?: DeepPartial<LoginResponse>): LoginResponse {
    return LoginResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LoginResponse>): LoginResponse {
    const message = createBaseLoginResponse();
    message.access_token = object.access_token ?? "";
    message.expires_in = object.expires_in ?? 0;
    message.user = (object.user !== undefined && object.user !== null) ? User.fromPartial(object.user) : undefined;
    return message;
  },
};

function createBaseLogoutRequest(): LogoutRequest {
  return {};
}

export const LogoutRequest: MessageFns<LogoutRequest> = {
  encode(_: LogoutRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogoutRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogoutRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): LogoutRequest {
    return {};
  },

  toJSON(_: LogoutRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<LogoutRequest>): LogoutRequest {
    return LogoutRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<LogoutRequest>): LogoutRequest {
    const message = createBaseLogoutRequest();
    return message;
  },
};

function createBaseLogoutResponse(): LogoutResponse {
  return {};
}

export const LogoutResponse: MessageFns<LogoutResponse> = {
  encode(_: LogoutResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogoutResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogoutResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): LogoutResponse {
    return {};
  },

  toJSON(_: LogoutResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<LogoutResponse>): LogoutResponse {
    return LogoutResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<LogoutResponse>): LogoutResponse {
    const message = createBaseLogoutResponse();
    return message;
  },
};

function createBaseMeRequest(): MeRequest {
  return {};
}

export const MeRequest: MessageFns<MeRequest> = {
  encode(_: MeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MeRequest {
    return {};
  },

  toJSON(_: MeRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MeRequest>): MeRequest {
    return MeRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MeRequest>): MeRequest {
    const message = createBaseMeRequest();
    return message;
  },
};

function createBaseMeResponse(): MeResponse {
  return { user: undefined };
}

export const MeResponse: MessageFns<MeResponse> = {
  encode(message: MeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MeResponse {
    return { user: isSet(object.user) ? User.fromJSON(object.user) : undefined };
  },

  toJSON(message: MeResponse): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    return obj;
  },

  create(base?: DeepPartial<MeResponse>): MeResponse {
    return MeResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MeResponse>): MeResponse {
    const message = createBaseMeResponse();
    message.user = (object.user !== undefined && object.user !== null) ? User.fromPartial(object.user) : undefined;
    return message;
  },
};

function createBaseRefreshRequest(): RefreshRequest {
  return {};
}

export const RefreshRequest: MessageFns<RefreshRequest> = {
  encode(_: RefreshRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefreshRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RefreshRequest {
    return {};
  },

  toJSON(_: RefreshRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<RefreshRequest>): RefreshRequest {
    return RefreshRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<RefreshRequest>): RefreshRequest {
    const message = createBaseRefreshRequest();
    return message;
  },
};

function createBaseRefreshResponse(): RefreshResponse {
  return { access_token: "", expires_in: 0, user: undefined };
}

export const RefreshResponse: MessageFns<RefreshResponse> = {
  encode(message: RefreshResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.access_token !== "") {
      writer.uint32(10).string(message.access_token);
    }
    if (message.expires_in !== 0) {
      writer.uint32(16).int32(message.expires_in);
    }
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefreshResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.access_token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expires_in = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefreshResponse {
    return {
      access_token: isSet(object.accessToken)
        ? globalThis.String(object.accessToken)
        : isSet(object.access_token)
        ? globalThis.String(object.access_token)
        : "",
      expires_in: isSet(object.expiresIn)
        ? globalThis.Number(object.expiresIn)
        : isSet(object.expires_in)
        ? globalThis.Number(object.expires_in)
        : 0,
      user: isSet(object.user) ? User.fromJSON(object.user) : undefined,
    };
  },

  toJSON(message: RefreshResponse): unknown {
    const obj: any = {};
    if (message.access_token !== "") {
      obj.accessToken = message.access_token;
    }
    if (message.expires_in !== 0) {
      obj.expiresIn = Math.round(message.expires_in);
    }
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    return obj;
  },

  create(base?: DeepPartial<RefreshResponse>): RefreshResponse {
    return RefreshResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RefreshResponse>): RefreshResponse {
    const message = createBaseRefreshResponse();
    message.access_token = object.access_token ?? "";
    message.expires_in = object.expires_in ?? 0;
    message.user = (object.user !== undefined && object.user !== null) ? User.fromPartial(object.user) : undefined;
    return message;
  },
};

function createBaseRegisterRequest(): RegisterRequest {
  return { email: "", email_code: "", password: "", name: "" };
}

export const RegisterRequest: MessageFns<RegisterRequest> = {
  encode(message: RegisterRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.email_code !== "") {
      writer.uint32(18).string(message.email_code);
    }
    if (message.password !== "") {
      writer.uint32(26).string(message.password);
    }
    if (message.name !== "") {
      writer.uint32(34).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.email_code = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.password = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterRequest {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      email_code: isSet(object.emailCode)
        ? globalThis.String(object.emailCode)
        : isSet(object.email_code)
        ? globalThis.String(object.email_code)
        : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
    };
  },

  toJSON(message: RegisterRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.email_code !== "") {
      obj.emailCode = message.email_code;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterRequest>): RegisterRequest {
    return RegisterRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterRequest>): RegisterRequest {
    const message = createBaseRegisterRequest();
    message.email = object.email ?? "";
    message.email_code = object.email_code ?? "";
    message.password = object.password ?? "";
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseRegisterResponse(): RegisterResponse {
  return {};
}

export const RegisterResponse: MessageFns<RegisterResponse> = {
  encode(_: RegisterResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RegisterResponse {
    return {};
  },

  toJSON(_: RegisterResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<RegisterResponse>): RegisterResponse {
    return RegisterResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<RegisterResponse>): RegisterResponse {
    const message = createBaseRegisterResponse();
    return message;
  },
};

function createBaseResetPasswordRequest(): ResetPasswordRequest {
  return { email: "", email_code: "", new_password: "" };
}

export const ResetPasswordRequest: MessageFns<ResetPasswordRequest> = {
  encode(message: ResetPasswordRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.email_code !== "") {
      writer.uint32(18).string(message.email_code);
    }
    if (message.new_password !== "") {
      writer.uint32(26).string(message.new_password);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResetPasswordRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResetPasswordRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.email_code = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.new_password = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResetPasswordRequest {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      email_code: isSet(object.emailCode)
        ? globalThis.String(object.emailCode)
        : isSet(object.email_code)
        ? globalThis.String(object.email_code)
        : "",
      new_password: isSet(object.newPassword)
        ? globalThis.String(object.newPassword)
        : isSet(object.new_password)
        ? globalThis.String(object.new_password)
        : "",
    };
  },

  toJSON(message: ResetPasswordRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.email_code !== "") {
      obj.emailCode = message.email_code;
    }
    if (message.new_password !== "") {
      obj.newPassword = message.new_password;
    }
    return obj;
  },

  create(base?: DeepPartial<ResetPasswordRequest>): ResetPasswordRequest {
    return ResetPasswordRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ResetPasswordRequest>): ResetPasswordRequest {
    const message = createBaseResetPasswordRequest();
    message.email = object.email ?? "";
    message.email_code = object.email_code ?? "";
    message.new_password = object.new_password ?? "";
    return message;
  },
};

function createBaseResetPasswordResponse(): ResetPasswordResponse {
  return {};
}

export const ResetPasswordResponse: MessageFns<ResetPasswordResponse> = {
  encode(_: ResetPasswordResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResetPasswordResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResetPasswordResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ResetPasswordResponse {
    return {};
  },

  toJSON(_: ResetPasswordResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ResetPasswordResponse>): ResetPasswordResponse {
    return ResetPasswordResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ResetPasswordResponse>): ResetPasswordResponse {
    const message = createBaseResetPasswordResponse();
    return message;
  },
};

function createBaseSendEmailCodeRequest(): SendEmailCodeRequest {
  return { email: "", scene: 0 };
}

export const SendEmailCodeRequest: MessageFns<SendEmailCodeRequest> = {
  encode(message: SendEmailCodeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.scene !== 0) {
      writer.uint32(16).int32(message.scene);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendEmailCodeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendEmailCodeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.scene = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendEmailCodeRequest {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      scene: isSet(object.scene) ? emailCodeScene_ValueFromJSON(object.scene) : 0,
    };
  },

  toJSON(message: SendEmailCodeRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.scene !== 0) {
      obj.scene = emailCodeScene_ValueToJSON(message.scene);
    }
    return obj;
  },

  create(base?: DeepPartial<SendEmailCodeRequest>): SendEmailCodeRequest {
    return SendEmailCodeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SendEmailCodeRequest>): SendEmailCodeRequest {
    const message = createBaseSendEmailCodeRequest();
    message.email = object.email ?? "";
    message.scene = object.scene ?? 0;
    return message;
  },
};

function createBaseSendEmailCodeResponse(): SendEmailCodeResponse {
  return { expires_in: 0 };
}

export const SendEmailCodeResponse: MessageFns<SendEmailCodeResponse> = {
  encode(message: SendEmailCodeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.expires_in !== 0) {
      writer.uint32(8).int32(message.expires_in);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendEmailCodeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendEmailCodeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.expires_in = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendEmailCodeResponse {
    return {
      expires_in: isSet(object.expiresIn)
        ? globalThis.Number(object.expiresIn)
        : isSet(object.expires_in)
        ? globalThis.Number(object.expires_in)
        : 0,
    };
  },

  toJSON(message: SendEmailCodeResponse): unknown {
    const obj: any = {};
    if (message.expires_in !== 0) {
      obj.expiresIn = Math.round(message.expires_in);
    }
    return obj;
  },

  create(base?: DeepPartial<SendEmailCodeResponse>): SendEmailCodeResponse {
    return SendEmailCodeResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SendEmailCodeResponse>): SendEmailCodeResponse {
    const message = createBaseSendEmailCodeResponse();
    message.expires_in = object.expires_in ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
