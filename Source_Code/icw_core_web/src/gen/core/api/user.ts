// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: core/api/user.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AvatarType_Value, avatarType_ValueFromJSON, avatarType_ValueToJSON } from "../common";

export const protobufPackage = "icw.core.api";

/** DeleteAvatar 请求结构体 */
export interface DeleteAvatarRequest {
}

/** DeleteAvatar 响应结构体 */
export interface DeleteAvatarResponse {
}

/** GetAvatar 请求结构体 */
export interface GetAvatarRequest {
}

/** GetAvatar 响应结构体 */
export interface GetAvatarResponse {
  avatar_url: string;
  avatar_type: AvatarType_Value;
}

/** UploadAvatar 请求结构体 */
export interface UploadAvatarRequest {
}

/** UploadAvatar 响应结构体 */
export interface UploadAvatarResponse {
  upload_url: string;
}

function createBaseDeleteAvatarRequest(): DeleteAvatarRequest {
  return {};
}

export const DeleteAvatarRequest: MessageFns<DeleteAvatarRequest> = {
  encode(_: DeleteAvatarRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteAvatarRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteAvatarRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DeleteAvatarRequest {
    return {};
  },

  toJSON(_: DeleteAvatarRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<DeleteAvatarRequest>): DeleteAvatarRequest {
    return DeleteAvatarRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<DeleteAvatarRequest>): DeleteAvatarRequest {
    const message = createBaseDeleteAvatarRequest();
    return message;
  },
};

function createBaseDeleteAvatarResponse(): DeleteAvatarResponse {
  return {};
}

export const DeleteAvatarResponse: MessageFns<DeleteAvatarResponse> = {
  encode(_: DeleteAvatarResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteAvatarResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteAvatarResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DeleteAvatarResponse {
    return {};
  },

  toJSON(_: DeleteAvatarResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<DeleteAvatarResponse>): DeleteAvatarResponse {
    return DeleteAvatarResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<DeleteAvatarResponse>): DeleteAvatarResponse {
    const message = createBaseDeleteAvatarResponse();
    return message;
  },
};

function createBaseGetAvatarRequest(): GetAvatarRequest {
  return {};
}

export const GetAvatarRequest: MessageFns<GetAvatarRequest> = {
  encode(_: GetAvatarRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAvatarRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAvatarRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetAvatarRequest {
    return {};
  },

  toJSON(_: GetAvatarRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetAvatarRequest>): GetAvatarRequest {
    return GetAvatarRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetAvatarRequest>): GetAvatarRequest {
    const message = createBaseGetAvatarRequest();
    return message;
  },
};

function createBaseGetAvatarResponse(): GetAvatarResponse {
  return { avatar_url: "", avatar_type: 0 };
}

export const GetAvatarResponse: MessageFns<GetAvatarResponse> = {
  encode(message: GetAvatarResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.avatar_url !== "") {
      writer.uint32(10).string(message.avatar_url);
    }
    if (message.avatar_type !== 0) {
      writer.uint32(16).int32(message.avatar_type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAvatarResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAvatarResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.avatar_url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.avatar_type = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAvatarResponse {
    return {
      avatar_url: isSet(object.avatarUrl)
        ? globalThis.String(object.avatarUrl)
        : isSet(object.avatar_url)
        ? globalThis.String(object.avatar_url)
        : "",
      avatar_type: isSet(object.avatarType)
        ? avatarType_ValueFromJSON(object.avatarType)
        : isSet(object.avatar_type)
        ? avatarType_ValueFromJSON(object.avatar_type)
        : 0,
    };
  },

  toJSON(message: GetAvatarResponse): unknown {
    const obj: any = {};
    if (message.avatar_url !== "") {
      obj.avatarUrl = message.avatar_url;
    }
    if (message.avatar_type !== 0) {
      obj.avatarType = avatarType_ValueToJSON(message.avatar_type);
    }
    return obj;
  },

  create(base?: DeepPartial<GetAvatarResponse>): GetAvatarResponse {
    return GetAvatarResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetAvatarResponse>): GetAvatarResponse {
    const message = createBaseGetAvatarResponse();
    message.avatar_url = object.avatar_url ?? "";
    message.avatar_type = object.avatar_type ?? 0;
    return message;
  },
};

function createBaseUploadAvatarRequest(): UploadAvatarRequest {
  return {};
}

export const UploadAvatarRequest: MessageFns<UploadAvatarRequest> = {
  encode(_: UploadAvatarRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadAvatarRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadAvatarRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): UploadAvatarRequest {
    return {};
  },

  toJSON(_: UploadAvatarRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<UploadAvatarRequest>): UploadAvatarRequest {
    return UploadAvatarRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<UploadAvatarRequest>): UploadAvatarRequest {
    const message = createBaseUploadAvatarRequest();
    return message;
  },
};

function createBaseUploadAvatarResponse(): UploadAvatarResponse {
  return { upload_url: "" };
}

export const UploadAvatarResponse: MessageFns<UploadAvatarResponse> = {
  encode(message: UploadAvatarResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.upload_url !== "") {
      writer.uint32(10).string(message.upload_url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadAvatarResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadAvatarResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.upload_url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadAvatarResponse {
    return {
      upload_url: isSet(object.uploadUrl)
        ? globalThis.String(object.uploadUrl)
        : isSet(object.upload_url)
        ? globalThis.String(object.upload_url)
        : "",
    };
  },

  toJSON(message: UploadAvatarResponse): unknown {
    const obj: any = {};
    if (message.upload_url !== "") {
      obj.uploadUrl = message.upload_url;
    }
    return obj;
  },

  create(base?: DeepPartial<UploadAvatarResponse>): UploadAvatarResponse {
    return UploadAvatarResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UploadAvatarResponse>): UploadAvatarResponse {
    const message = createBaseUploadAvatarResponse();
    message.upload_url = object.upload_url ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
