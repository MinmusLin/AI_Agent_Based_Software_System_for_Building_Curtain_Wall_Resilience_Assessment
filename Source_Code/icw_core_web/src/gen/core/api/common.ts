// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: core/api/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  ProjectImage,
  ProjectProgress_Value,
  projectProgress_ValueFromJSON,
  projectProgress_ValueToJSON,
} from "../common";

export const protobufPackage = "icw.core.api";

/** 项目信息结构体 */
export interface Project {
  id: string;
  name: string;
  building_name: string;
  building_location: string;
  built_year: number;
  building_description: string;
  known_issues: string;
  assessment_goal: string;
  thumbnail_url: string;
  progress: ProjectProgress_Value;
  created_at: string;
  updated_at: string;
}

/** 项目列表项结构体 */
export interface ProjectListItem {
  id: string;
  name: string;
  building_name: string;
  building_location: string;
  thumbnail_url: string;
  progress: ProjectProgress_Value;
  created_at: string;
}

/** 项目图像组结构体 */
export interface ProjectGroup {
  id: string;
  name: string;
  sort_order: string;
  images: ProjectImage[];
}

/** 项目图像上传项结构体 */
export interface UploadProjectImageItem {
  file_name: string;
  content_type: string;
  size_bytes: number;
  width: number;
  height: number;
  metadata: string;
}

function createBaseProject(): Project {
  return {
    id: "",
    name: "",
    building_name: "",
    building_location: "",
    built_year: 0,
    building_description: "",
    known_issues: "",
    assessment_goal: "",
    thumbnail_url: "",
    progress: 0,
    created_at: "",
    updated_at: "",
  };
}

export const Project: MessageFns<Project> = {
  encode(message: Project, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.building_name !== "") {
      writer.uint32(26).string(message.building_name);
    }
    if (message.building_location !== "") {
      writer.uint32(34).string(message.building_location);
    }
    if (message.built_year !== 0) {
      writer.uint32(40).uint32(message.built_year);
    }
    if (message.building_description !== "") {
      writer.uint32(50).string(message.building_description);
    }
    if (message.known_issues !== "") {
      writer.uint32(58).string(message.known_issues);
    }
    if (message.assessment_goal !== "") {
      writer.uint32(66).string(message.assessment_goal);
    }
    if (message.thumbnail_url !== "") {
      writer.uint32(74).string(message.thumbnail_url);
    }
    if (message.progress !== 0) {
      writer.uint32(80).int32(message.progress);
    }
    if (message.created_at !== "") {
      writer.uint32(90).string(message.created_at);
    }
    if (message.updated_at !== "") {
      writer.uint32(98).string(message.updated_at);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Project {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProject();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.building_name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.building_location = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.built_year = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.building_description = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.known_issues = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.assessment_goal = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.thumbnail_url = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.progress = reader.int32() as any;
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.created_at = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.updated_at = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Project {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      building_name: isSet(object.buildingName)
        ? globalThis.String(object.buildingName)
        : isSet(object.building_name)
        ? globalThis.String(object.building_name)
        : "",
      building_location: isSet(object.buildingLocation)
        ? globalThis.String(object.buildingLocation)
        : isSet(object.building_location)
        ? globalThis.String(object.building_location)
        : "",
      built_year: isSet(object.builtYear)
        ? globalThis.Number(object.builtYear)
        : isSet(object.built_year)
        ? globalThis.Number(object.built_year)
        : 0,
      building_description: isSet(object.buildingDescription)
        ? globalThis.String(object.buildingDescription)
        : isSet(object.building_description)
        ? globalThis.String(object.building_description)
        : "",
      known_issues: isSet(object.knownIssues)
        ? globalThis.String(object.knownIssues)
        : isSet(object.known_issues)
        ? globalThis.String(object.known_issues)
        : "",
      assessment_goal: isSet(object.assessmentGoal)
        ? globalThis.String(object.assessmentGoal)
        : isSet(object.assessment_goal)
        ? globalThis.String(object.assessment_goal)
        : "",
      thumbnail_url: isSet(object.thumbnailUrl)
        ? globalThis.String(object.thumbnailUrl)
        : isSet(object.thumbnail_url)
        ? globalThis.String(object.thumbnail_url)
        : "",
      progress: isSet(object.progress) ? projectProgress_ValueFromJSON(object.progress) : 0,
      created_at: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      updated_at: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
    };
  },

  toJSON(message: Project): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.building_name !== "") {
      obj.buildingName = message.building_name;
    }
    if (message.building_location !== "") {
      obj.buildingLocation = message.building_location;
    }
    if (message.built_year !== 0) {
      obj.builtYear = Math.round(message.built_year);
    }
    if (message.building_description !== "") {
      obj.buildingDescription = message.building_description;
    }
    if (message.known_issues !== "") {
      obj.knownIssues = message.known_issues;
    }
    if (message.assessment_goal !== "") {
      obj.assessmentGoal = message.assessment_goal;
    }
    if (message.thumbnail_url !== "") {
      obj.thumbnailUrl = message.thumbnail_url;
    }
    if (message.progress !== 0) {
      obj.progress = projectProgress_ValueToJSON(message.progress);
    }
    if (message.created_at !== "") {
      obj.createdAt = message.created_at;
    }
    if (message.updated_at !== "") {
      obj.updatedAt = message.updated_at;
    }
    return obj;
  },

  create(base?: DeepPartial<Project>): Project {
    return Project.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Project>): Project {
    const message = createBaseProject();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.building_name = object.building_name ?? "";
    message.building_location = object.building_location ?? "";
    message.built_year = object.built_year ?? 0;
    message.building_description = object.building_description ?? "";
    message.known_issues = object.known_issues ?? "";
    message.assessment_goal = object.assessment_goal ?? "";
    message.thumbnail_url = object.thumbnail_url ?? "";
    message.progress = object.progress ?? 0;
    message.created_at = object.created_at ?? "";
    message.updated_at = object.updated_at ?? "";
    return message;
  },
};

function createBaseProjectListItem(): ProjectListItem {
  return { id: "", name: "", building_name: "", building_location: "", thumbnail_url: "", progress: 0, created_at: "" };
}

export const ProjectListItem: MessageFns<ProjectListItem> = {
  encode(message: ProjectListItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.building_name !== "") {
      writer.uint32(26).string(message.building_name);
    }
    if (message.building_location !== "") {
      writer.uint32(34).string(message.building_location);
    }
    if (message.thumbnail_url !== "") {
      writer.uint32(42).string(message.thumbnail_url);
    }
    if (message.progress !== 0) {
      writer.uint32(48).int32(message.progress);
    }
    if (message.created_at !== "") {
      writer.uint32(58).string(message.created_at);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectListItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectListItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.building_name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.building_location = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.thumbnail_url = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.progress = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.created_at = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProjectListItem {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      building_name: isSet(object.buildingName)
        ? globalThis.String(object.buildingName)
        : isSet(object.building_name)
        ? globalThis.String(object.building_name)
        : "",
      building_location: isSet(object.buildingLocation)
        ? globalThis.String(object.buildingLocation)
        : isSet(object.building_location)
        ? globalThis.String(object.building_location)
        : "",
      thumbnail_url: isSet(object.thumbnailUrl)
        ? globalThis.String(object.thumbnailUrl)
        : isSet(object.thumbnail_url)
        ? globalThis.String(object.thumbnail_url)
        : "",
      progress: isSet(object.progress) ? projectProgress_ValueFromJSON(object.progress) : 0,
      created_at: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
    };
  },

  toJSON(message: ProjectListItem): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.building_name !== "") {
      obj.buildingName = message.building_name;
    }
    if (message.building_location !== "") {
      obj.buildingLocation = message.building_location;
    }
    if (message.thumbnail_url !== "") {
      obj.thumbnailUrl = message.thumbnail_url;
    }
    if (message.progress !== 0) {
      obj.progress = projectProgress_ValueToJSON(message.progress);
    }
    if (message.created_at !== "") {
      obj.createdAt = message.created_at;
    }
    return obj;
  },

  create(base?: DeepPartial<ProjectListItem>): ProjectListItem {
    return ProjectListItem.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProjectListItem>): ProjectListItem {
    const message = createBaseProjectListItem();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.building_name = object.building_name ?? "";
    message.building_location = object.building_location ?? "";
    message.thumbnail_url = object.thumbnail_url ?? "";
    message.progress = object.progress ?? 0;
    message.created_at = object.created_at ?? "";
    return message;
  },
};

function createBaseProjectGroup(): ProjectGroup {
  return { id: "", name: "", sort_order: "", images: [] };
}

export const ProjectGroup: MessageFns<ProjectGroup> = {
  encode(message: ProjectGroup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.sort_order !== "") {
      writer.uint32(26).string(message.sort_order);
    }
    for (const v of message.images) {
      ProjectImage.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectGroup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sort_order = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.images.push(ProjectImage.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProjectGroup {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      sort_order: isSet(object.sortOrder)
        ? globalThis.String(object.sortOrder)
        : isSet(object.sort_order)
        ? globalThis.String(object.sort_order)
        : "",
      images: globalThis.Array.isArray(object?.images) ? object.images.map((e: any) => ProjectImage.fromJSON(e)) : [],
    };
  },

  toJSON(message: ProjectGroup): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.sort_order !== "") {
      obj.sortOrder = message.sort_order;
    }
    if (message.images?.length) {
      obj.images = message.images.map((e) => ProjectImage.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ProjectGroup>): ProjectGroup {
    return ProjectGroup.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProjectGroup>): ProjectGroup {
    const message = createBaseProjectGroup();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.sort_order = object.sort_order ?? "";
    message.images = object.images?.map((e) => ProjectImage.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUploadProjectImageItem(): UploadProjectImageItem {
  return { file_name: "", content_type: "", size_bytes: 0, width: 0, height: 0, metadata: "" };
}

export const UploadProjectImageItem: MessageFns<UploadProjectImageItem> = {
  encode(message: UploadProjectImageItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.file_name !== "") {
      writer.uint32(10).string(message.file_name);
    }
    if (message.content_type !== "") {
      writer.uint32(18).string(message.content_type);
    }
    if (message.size_bytes !== 0) {
      writer.uint32(24).uint64(message.size_bytes);
    }
    if (message.width !== 0) {
      writer.uint32(32).uint32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(40).uint32(message.height);
    }
    if (message.metadata !== "") {
      writer.uint32(50).string(message.metadata);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadProjectImageItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadProjectImageItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.file_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content_type = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.size_bytes = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.width = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.height = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.metadata = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadProjectImageItem {
    return {
      file_name: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
      content_type: isSet(object.contentType)
        ? globalThis.String(object.contentType)
        : isSet(object.content_type)
        ? globalThis.String(object.content_type)
        : "",
      size_bytes: isSet(object.sizeBytes)
        ? globalThis.Number(object.sizeBytes)
        : isSet(object.size_bytes)
        ? globalThis.Number(object.size_bytes)
        : 0,
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      metadata: isSet(object.metadata) ? globalThis.String(object.metadata) : "",
    };
  },

  toJSON(message: UploadProjectImageItem): unknown {
    const obj: any = {};
    if (message.file_name !== "") {
      obj.fileName = message.file_name;
    }
    if (message.content_type !== "") {
      obj.contentType = message.content_type;
    }
    if (message.size_bytes !== 0) {
      obj.sizeBytes = Math.round(message.size_bytes);
    }
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.metadata !== "") {
      obj.metadata = message.metadata;
    }
    return obj;
  },

  create(base?: DeepPartial<UploadProjectImageItem>): UploadProjectImageItem {
    return UploadProjectImageItem.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UploadProjectImageItem>): UploadProjectImageItem {
    const message = createBaseUploadProjectImageItem();
    message.file_name = object.file_name ?? "";
    message.content_type = object.content_type ?? "";
    message.size_bytes = object.size_bytes ?? 0;
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    message.metadata = object.metadata ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
