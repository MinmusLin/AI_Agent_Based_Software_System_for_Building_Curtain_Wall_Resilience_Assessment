// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: core/api/project_core.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ProjectProgress_Value, projectProgress_ValueFromJSON, projectProgress_ValueToJSON } from "../common";
import { Project, ProjectListItem } from "./common";

export const protobufPackage = "icw.core.api";

/** AdvanceProject 请求结构体 */
export interface AdvanceProjectRequest {
  project_id: string;
  from_progress: ProjectProgress_Value;
  to_progress: ProjectProgress_Value;
}

/** AdvanceProject 响应结构体 */
export interface AdvanceProjectResponse {
}

/** CreateProject 请求结构体 */
export interface CreateProjectRequest {
}

/** CreateProject 响应结构体 */
export interface CreateProjectResponse {
  project: Project | undefined;
}

/** DeleteProject 请求结构体 */
export interface DeleteProjectRequest {
  project_id: string;
}

/** DeleteProject 响应结构体 */
export interface DeleteProjectResponse {
  active_projects: ProjectListItem[];
  completed_projects: ProjectListItem[];
}

/** ListProjects 请求结构体 */
export interface ListProjectsRequest {
}

/** ListProjects 响应结构体 */
export interface ListProjectsResponse {
  active_projects: ProjectListItem[];
  completed_projects: ProjectListItem[];
}

function createBaseAdvanceProjectRequest(): AdvanceProjectRequest {
  return { project_id: "", from_progress: 0, to_progress: 0 };
}

export const AdvanceProjectRequest: MessageFns<AdvanceProjectRequest> = {
  encode(message: AdvanceProjectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project_id !== "") {
      writer.uint32(10).string(message.project_id);
    }
    if (message.from_progress !== 0) {
      writer.uint32(16).int32(message.from_progress);
    }
    if (message.to_progress !== 0) {
      writer.uint32(24).int32(message.to_progress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AdvanceProjectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAdvanceProjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.from_progress = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.to_progress = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AdvanceProjectRequest {
    return {
      project_id: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
      from_progress: isSet(object.fromProgress)
        ? projectProgress_ValueFromJSON(object.fromProgress)
        : isSet(object.from_progress)
        ? projectProgress_ValueFromJSON(object.from_progress)
        : 0,
      to_progress: isSet(object.toProgress)
        ? projectProgress_ValueFromJSON(object.toProgress)
        : isSet(object.to_progress)
        ? projectProgress_ValueFromJSON(object.to_progress)
        : 0,
    };
  },

  toJSON(message: AdvanceProjectRequest): unknown {
    const obj: any = {};
    if (message.project_id !== "") {
      obj.projectId = message.project_id;
    }
    if (message.from_progress !== 0) {
      obj.fromProgress = projectProgress_ValueToJSON(message.from_progress);
    }
    if (message.to_progress !== 0) {
      obj.toProgress = projectProgress_ValueToJSON(message.to_progress);
    }
    return obj;
  },

  create(base?: DeepPartial<AdvanceProjectRequest>): AdvanceProjectRequest {
    return AdvanceProjectRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AdvanceProjectRequest>): AdvanceProjectRequest {
    const message = createBaseAdvanceProjectRequest();
    message.project_id = object.project_id ?? "";
    message.from_progress = object.from_progress ?? 0;
    message.to_progress = object.to_progress ?? 0;
    return message;
  },
};

function createBaseAdvanceProjectResponse(): AdvanceProjectResponse {
  return {};
}

export const AdvanceProjectResponse: MessageFns<AdvanceProjectResponse> = {
  encode(_: AdvanceProjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AdvanceProjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAdvanceProjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): AdvanceProjectResponse {
    return {};
  },

  toJSON(_: AdvanceProjectResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<AdvanceProjectResponse>): AdvanceProjectResponse {
    return AdvanceProjectResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<AdvanceProjectResponse>): AdvanceProjectResponse {
    const message = createBaseAdvanceProjectResponse();
    return message;
  },
};

function createBaseCreateProjectRequest(): CreateProjectRequest {
  return {};
}

export const CreateProjectRequest: MessageFns<CreateProjectRequest> = {
  encode(_: CreateProjectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateProjectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateProjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CreateProjectRequest {
    return {};
  },

  toJSON(_: CreateProjectRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<CreateProjectRequest>): CreateProjectRequest {
    return CreateProjectRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<CreateProjectRequest>): CreateProjectRequest {
    const message = createBaseCreateProjectRequest();
    return message;
  },
};

function createBaseCreateProjectResponse(): CreateProjectResponse {
  return { project: undefined };
}

export const CreateProjectResponse: MessageFns<CreateProjectResponse> = {
  encode(message: CreateProjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project !== undefined) {
      Project.encode(message.project, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateProjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateProjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project = Project.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateProjectResponse {
    return { project: isSet(object.project) ? Project.fromJSON(object.project) : undefined };
  },

  toJSON(message: CreateProjectResponse): unknown {
    const obj: any = {};
    if (message.project !== undefined) {
      obj.project = Project.toJSON(message.project);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateProjectResponse>): CreateProjectResponse {
    return CreateProjectResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateProjectResponse>): CreateProjectResponse {
    const message = createBaseCreateProjectResponse();
    message.project = (object.project !== undefined && object.project !== null)
      ? Project.fromPartial(object.project)
      : undefined;
    return message;
  },
};

function createBaseDeleteProjectRequest(): DeleteProjectRequest {
  return { project_id: "" };
}

export const DeleteProjectRequest: MessageFns<DeleteProjectRequest> = {
  encode(message: DeleteProjectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project_id !== "") {
      writer.uint32(10).string(message.project_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteProjectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteProjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteProjectRequest {
    return {
      project_id: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
    };
  },

  toJSON(message: DeleteProjectRequest): unknown {
    const obj: any = {};
    if (message.project_id !== "") {
      obj.projectId = message.project_id;
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteProjectRequest>): DeleteProjectRequest {
    return DeleteProjectRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteProjectRequest>): DeleteProjectRequest {
    const message = createBaseDeleteProjectRequest();
    message.project_id = object.project_id ?? "";
    return message;
  },
};

function createBaseDeleteProjectResponse(): DeleteProjectResponse {
  return { active_projects: [], completed_projects: [] };
}

export const DeleteProjectResponse: MessageFns<DeleteProjectResponse> = {
  encode(message: DeleteProjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.active_projects) {
      ProjectListItem.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.completed_projects) {
      ProjectListItem.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteProjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteProjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.active_projects.push(ProjectListItem.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.completed_projects.push(ProjectListItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteProjectResponse {
    return {
      active_projects: globalThis.Array.isArray(object?.activeProjects)
        ? object.activeProjects.map((e: any) => ProjectListItem.fromJSON(e))
        : globalThis.Array.isArray(object?.active_projects)
        ? object.active_projects.map((e: any) => ProjectListItem.fromJSON(e))
        : [],
      completed_projects: globalThis.Array.isArray(object?.completedProjects)
        ? object.completedProjects.map((e: any) => ProjectListItem.fromJSON(e))
        : globalThis.Array.isArray(object?.completed_projects)
        ? object.completed_projects.map((e: any) => ProjectListItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DeleteProjectResponse): unknown {
    const obj: any = {};
    if (message.active_projects?.length) {
      obj.activeProjects = message.active_projects.map((e) => ProjectListItem.toJSON(e));
    }
    if (message.completed_projects?.length) {
      obj.completedProjects = message.completed_projects.map((e) => ProjectListItem.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteProjectResponse>): DeleteProjectResponse {
    return DeleteProjectResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteProjectResponse>): DeleteProjectResponse {
    const message = createBaseDeleteProjectResponse();
    message.active_projects = object.active_projects?.map((e) => ProjectListItem.fromPartial(e)) || [];
    message.completed_projects = object.completed_projects?.map((e) => ProjectListItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseListProjectsRequest(): ListProjectsRequest {
  return {};
}

export const ListProjectsRequest: MessageFns<ListProjectsRequest> = {
  encode(_: ListProjectsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListProjectsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListProjectsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListProjectsRequest {
    return {};
  },

  toJSON(_: ListProjectsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ListProjectsRequest>): ListProjectsRequest {
    return ListProjectsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListProjectsRequest>): ListProjectsRequest {
    const message = createBaseListProjectsRequest();
    return message;
  },
};

function createBaseListProjectsResponse(): ListProjectsResponse {
  return { active_projects: [], completed_projects: [] };
}

export const ListProjectsResponse: MessageFns<ListProjectsResponse> = {
  encode(message: ListProjectsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.active_projects) {
      ProjectListItem.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.completed_projects) {
      ProjectListItem.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListProjectsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListProjectsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.active_projects.push(ProjectListItem.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.completed_projects.push(ProjectListItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListProjectsResponse {
    return {
      active_projects: globalThis.Array.isArray(object?.activeProjects)
        ? object.activeProjects.map((e: any) => ProjectListItem.fromJSON(e))
        : globalThis.Array.isArray(object?.active_projects)
        ? object.active_projects.map((e: any) => ProjectListItem.fromJSON(e))
        : [],
      completed_projects: globalThis.Array.isArray(object?.completedProjects)
        ? object.completedProjects.map((e: any) => ProjectListItem.fromJSON(e))
        : globalThis.Array.isArray(object?.completed_projects)
        ? object.completed_projects.map((e: any) => ProjectListItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListProjectsResponse): unknown {
    const obj: any = {};
    if (message.active_projects?.length) {
      obj.activeProjects = message.active_projects.map((e) => ProjectListItem.toJSON(e));
    }
    if (message.completed_projects?.length) {
      obj.completedProjects = message.completed_projects.map((e) => ProjectListItem.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListProjectsResponse>): ListProjectsResponse {
    return ListProjectsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListProjectsResponse>): ListProjectsResponse {
    const message = createBaseListProjectsResponse();
    message.active_projects = object.active_projects?.map((e) => ProjectListItem.fromPartial(e)) || [];
    message.completed_projects = object.completed_projects?.map((e) => ProjectListItem.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
