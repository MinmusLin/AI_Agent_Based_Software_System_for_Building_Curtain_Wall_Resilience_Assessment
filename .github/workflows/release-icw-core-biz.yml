name: Release icw_core_biz

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - Source_Code/icw_core_biz/**
      - Source_Code/icw_common/**
      - .github/workflows/release-icw-server.yml
      - .github/workflows/release-icw-core-biz.yml

jobs:
  release:
    permissions:
      contents: write
    uses: ./.github/workflows/release-icw-server.yml
    with:
      service: icw_core_biz
